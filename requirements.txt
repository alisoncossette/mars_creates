# Core
anthropic>=0.40.0          # captions (Claude)
python-dotenv>=1.0.1
httpx>=0.27.0              # Magnific API (api.magnific.com)

# Publish
tweepy>=4.16              # X / Twitter

# On-robot only (already present in the Innate env): opencv-python for the optional camera grab
